const protocol = 'http';
const host = 'https://backend.oramsysdev.com/';
// const host = 'http://192.168.1.52:5002/';
// const port = '5002';
// const trailUrl = 'api/v1';

// const hostUrl = `${protocol}://${host}${port ? ':' + port : ''}/`;
// const endpoint = `${protocol}://${host}${(port ? ':' + port : '')}/`;
// eslint-disable-next-line
export default {
    // protocol: protocol,
    host: host,
    // port: port,
    // apiUrl: trailUrl,
    // endpoint: endpoint,
    // hostUrl: hostUrl
};